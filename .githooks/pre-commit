set -euo pipefail

git diff --cached --name-only | grep -Eq \
    '^(VERSION|CHANGELOG\.md|bindings/python/pyproject\.toml|extern/geozl)$' \
    || exit 0

root=$(git rev-parse --show-toplevel)
python3 "$root/tools/check_release.py"
